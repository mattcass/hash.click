;(function ( $, w, doc ) {

  // Enable strict mode
  'use strict';

  $('.btn').on('click', function () {
    $('.box').attr('style', 'background: tomato;')
  });


})( jQuery, this, this.document );
